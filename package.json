{
  "name": "folio-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter demo dev",
    "build": "pnpm --filter demo build",
    "typecheck": "pnpm -r typecheck"
  }
}
